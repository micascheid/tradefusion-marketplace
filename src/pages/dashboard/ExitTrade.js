import { React } from 'react';

import { Box } from '@mui/material'

const ExitTrade = (props) => {
  const botName = props.bot_vals["bot_name"];





  return (
    <Box>
      "hello there
    </Box>
  )
}

export default ExitTrade