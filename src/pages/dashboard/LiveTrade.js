import {React} from 'react';
import {doc, getDoc} from 'firebase/firestore';
import {db} from '../../FirebaseConfig';
import {Box} from "@mui/material";

const LiveTrade = (props) => {

  return (
    <Box>
      Check Me out... I'm a box
    </Box>
  )
}

export default LiveTrade